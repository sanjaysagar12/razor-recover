services:
  webhook-receiver:
    build: .
    ports:
      - "5555:5555"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
      - ./models:/app/models
      - ./data:/app/data
    restart: unless-stopped
